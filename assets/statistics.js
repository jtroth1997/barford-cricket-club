(()=>{const data=window.BARFORD_DATA;let view='season',discipline='batting',query='';const content=document.querySelector('#stats-content'),season=document.querySelector('#season-select'),search=document.querySelector('#player-search');
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));
const labels={player:'Player',innings:'Inn',notOut:'NO',runs:'Runs',highScore:'HS',average:'Avg',balls:'Balls',strikeRate:'SR',fours:'4s',sixes:'6s',overs:'Overs',maidens:'Mdns',wickets:'Wkts',best:'Best',economy:'Econ',matches:'Mat',hundreds:'100s',fifties:'50s',fiveWickets:'5W'};
const highlights=document.querySelector('#highlights');highlights.innerHTML=data.records.slice(0,4).map(x=>`<article class="highlight"><small>${esc(x.label)}</small><strong>${esc(x.value)}</strong><span>${esc(x.detail.split('·')[0])}</span></article>`).join('');
Object.keys(data.seasons).sort((a,b)=>b-a).forEach(y=>season.add(new Option(y,y)));
function table(rows,cols){const filtered=rows.filter(r=>!query||r.player.toLowerCase().includes(query));if(!filtered.length)return '<div class="empty-state">No matching player found.</div>';return `<div class="stat-table-wrap"><table class="stat-table"><thead><tr>${cols.map(c=>`<th>${labels[c]||c}</th>`).join('')}</tr></thead><tbody>${filtered.map((r,i)=>`<tr>${cols.map((c,j)=>`<td>${j===0?`<span class="rank">${i+1}</span>`:''}${esc(r[c])}</td>`).join('')}</tr>`).join('')}</tbody></table></div>`}
function render(){document.querySelector('.season-controls').hidden=view!=='season';search.hidden=view==='records';if(view==='records'){content.innerHTML=`<div class="record-grid">${data.records.map(r=>`<article class="record-card"><small>${esc(r.label)}</small><strong>${esc(r.value)}</strong><p>${esc(r.detail)}</p></article>`).join('')}</div>`;return}let rows,cols;if(view==='season'){rows=data.seasons[season.value][discipline];cols=discipline==='batting'?['player','innings','notOut','runs','highScore','average','balls','strikeRate','fours','sixes']:['player','overs','maidens','runs','wickets','best','average','economy','strikeRate']}else if(view==='batting'){rows=data.allTimeBatting;cols=['player','matches','innings','runs','highScore','average','hundreds','fifties']}else{rows=data.allTimeBowling;cols=['player','matches','overs','wickets','best','average','economy','fiveWickets']}content.innerHTML=table(rows,cols)}
document.querySelectorAll('.tabs button').forEach(b=>b.onclick=()=>{document.querySelectorAll('.tabs button').forEach(x=>x.classList.remove('active'));b.classList.add('active');view=b.dataset.view;render()});document.querySelectorAll('.discipline button').forEach(b=>b.onclick=()=>{document.querySelectorAll('.discipline button').forEach(x=>x.classList.remove('active'));b.classList.add('active');discipline=b.dataset.discipline;render()});season.onchange=render;search.oninput=()=>{query=search.value.trim().toLowerCase();render()};render()})();
