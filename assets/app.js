document.querySelectorAll('.menu-button').forEach(button=>button.addEventListener('click',()=>{const nav=document.querySelector('.mobile-nav');if(!nav)return;const open=nav.classList.toggle('open');button.setAttribute('aria-expanded',open)}));
document.querySelectorAll('.mobile-nav a').forEach(a=>a.addEventListener('click',()=>document.querySelector('.mobile-nav')?.classList.remove('open')));
document.querySelectorAll('#year').forEach(el=>el.textContent=new Date().getFullYear());
const leaders=document.querySelector('#home-leaders');if(leaders){const rows=window.BARFORD_DATA.allTimeBatting.slice(0,5);leaders.innerHTML=rows.map(r=>`<li><b>${r.player}</b><span>${r.runs} runs</span></li>`).join('')}
async function loadClubNews(){const cfg=window.BARFORD_CONFIG||{},grid=document.querySelector('#news-grid');if(!grid||!cfg.supabaseUrl||!cfg.supabaseAnonKey||!window.supabase)return;const client=supabase.createClient(cfg.supabaseUrl,cfg.supabaseAnonKey),{data}=await client.from('news').select('*').eq('is_published',true).order('published_at',{ascending:false}).limit(3);if(!data?.length)return;const safe=v=>String(v||'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));grid.innerHTML=data.map((n,i)=>`<article class="${i===0?'news-main':'news-card'}">${n.image_url?`<img src="${safe(n.image_url)}" alt="" loading="lazy">`:''}<div><small>${safe(n.category)}</small><h3>${safe(n.title)}</h3><p>${safe(n.body).slice(0,180)}${n.body.length>180?'…':''}</p></div></article>`).join('')}loadClubNews();
